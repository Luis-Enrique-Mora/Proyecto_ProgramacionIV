import { Identifiers } from '@angular/compiler';

export interface Accounts {
    moneyAccountId: number;
    nameMoneyAccount: string;
    globalAmount: number;
    descriptionMoneyAccount: string;
}
